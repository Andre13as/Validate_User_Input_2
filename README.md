# Validate_User_Input_2
Small C++ code to validate if user input is correct.
This is a slightly imporved version of my C++ code to validate user input.
The code first checks if y or n is entered by the user. Only y or n is accepted.
If y is entered, the user is asked to enter a positive integer number of max. 3 digits. 
